spring:
  application:
    name: justina
  datasource:
    url: ${HOST}:${PORT}/${DBNAME}?createDatabaseIfNotExist=true
    username: ${USER}
    password: ${PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
api:
  base-url: api/v1